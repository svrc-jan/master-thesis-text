\chapter{Results}
\label{chap:results}

\subsubsection{Software and~hardware}
The implementation was done in \emph{C++} language \cite{cpp23}, using \emph{Gurobi} \cite{gurobi} as~the~MILP solver for both routing and~conflict resolution. The~source code is available from~\url{https://github.com/svrc-jan/train-opt}. All computations were done on a~computer with 
i7-9700F CPU and~16 GB of~RAM. 

\subsubsection{Instance grouping}
Because the~DISPLIB has 112 instances, we split them into 10 groups with approximately the~same size (9 to~16 instance per group) based in their initial source (encoded in the~name), which also corresponds to~the~structure of~the~problem. Unless specified otherwise, the~presented results will be the~median across the~instances inside the~group, we feel that this number should be a~good representation of~the~results as~it will not be skewed by~a~single instance within the~group and~thus provide us with a~valid overview that can still be distinguished between the~groups.	

\section{Aggregation}
To gauge how well did the~aggregation of~the~instance data work, table \ref{tab:instance} presents the~initial instance data counts. We can see that the~instances vary in size, with respect to~the~number of~trains, operations, resources, etc. The~most significant is the~difference between the~number of~operations compared to~the~number of~operation to~resource relations (ORR, sum of~the~number of~required resources for each operation). While the~\emph{nor} instances assign at~most one resource to~a~single operation, the~other instances allow multiple resources per operation.

\begin{table}[htbp!]
\begin{center}
\caption{Initial instance data}
\label{tab:instance}
\input{tables/instance_tab.tex}
\end{center}
\end{table}

Table \ref{tab:preprocess} shows the~aggregation counts, and~the~relative reductions compared to~the~initial data. We provide the~relative improvements to~see how well the~aggregation procedure reduced the~count of~the~required objects. For example, the~operation per level ratio gives how many fewer vertices we need in the~event graph compared to~using each operation as~a~vertex, i.e. how much we can parallelize the~events. Similarly, the~objective per route ratio gives how many fewer routing decisions we need to~make compared to~naively having a~decision (most likely a~binary variable) for each operation. The~most impactful is probably the~ORR per chunk ratio, as~grouping the~related resource conflicts is going to~have a~direct impact on how many conflicts we need to~resolve to~get a~conflict free solution.

\begin{table}[htbp!]
\begin{center}
\caption{Aggregation counts and~relative reductions}
\label{tab:preprocess}
\input{tables/preprocess_tab.tex}
\end{center}
\end{table}

Table \ref{tab:preprocess_opt} shows additional percentages for the~relevant instance group. First is the~invalid transition junctions. It shows what percentage of~junctions has an invalid transition; this is useful to~see how many additional constraints we will need to~make to~prevent the~invalid transitions. Second is the~optional level. This is the~maximum percentage of~levels that can be bypassed by~an operation, it might be useful to~choose another way of~encoding the~parallel events if there are too many optional levels. Third and~forth are the~direct and~parallel merges, telling us how effective each strategy was at~merging ORR to~chunks. The~\emph{nor} instance groups are omitted from~table \ref{tab:preprocess_opt}, as~they have all of~these values at~zero.

\begin{table}[htbp!]
\begin{center}
\caption{Aggregation - additional data}
\label{tab:preprocess_opt}
\input{tables/preprocess_opt_tab.tex}
\end{center}
\end{table}

\section{Routing and~conflict resolution}

We set the~timeout for the~conflict resolution at~10 minutes, as~was the~initial value set by~the~DISPLIB. Table \ref{tab:solve} gives multiple improvement percentages, comparing random and~optimized routing assignments. First is how much was the~total routing loss reduced by~the~routing procedure, second is how the~final objective improved, and~last is how much faster (or slower if negative) we arrived at~a~conflict free solution. It also includes the~gap to~the~best-known solution. The~last value is the~absolute time to~the~first conflict free solution to~show the~speed of~the~resolver. If conflict resolution procedure for the~random assignment failed to~solve the~instance, we set the~improvement as~100\%.

\begin{table}[htbp!]
\begin{center}
\caption{Routing and~conflict resolution}
\label{tab:solve}
\input{tables/solve_tab.tex}
\end{center}
\end{table}

It is important to~note that our conflict resolution procedure failed to~solve 1 instance from~the~\emph{nor4} group, 3 instances from~the~\emph{swi} group and~failed on all instances in the~\emph{nor5} and~\emph{wab} groups, within the~10 minutes. In~total, we managed to~solve 60\% of~the~instances. In~the~solved instances, 60\% of~these were solved in 1 second from~the~start, and~42\% were solved within 100 milliseconds.

All the~best-known solutions from~\emph{swi} group have zero delay, this is why the~best-known gap in \ref{tab:solve} for \emph{swi} is 100\%, even though our solutions have small delays, relative to~the~overall train durations.

\section{Time requirements}
Table \ref{tab:time_req} shows how much time each step of~the~initial stage before conflict resolution takes. This includes loading of~the~instance, aggregation of~the~instance data, creation of~the~link graph and~then the~routing optimization.

\begin{table}[htbp!]
\begin{center}
\caption{Time requirements for initial stage}
\label{tab:time_req}
\input{tables/time_tab.tex}
\end{center}
\textit{Note:} All values are in milliseconds.
\end{table}